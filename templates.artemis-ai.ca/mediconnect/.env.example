# MediConnect Template Environment Variables

# Database
DATABASE_URL="postgresql://postgres:password@db:5432/mediconnect?schema=public" # Default for Docker setup

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000" # Default Next.js dev URL
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET_HERE_IN_DEVELOPMENT" # Change this in .env for production!

# Ollama
OLLAMA_API_BASE_URL="http://localhost:11434" # Standard local Ollama URL
# OLLAMA_URL below is an older variable name, keeping it for now if used elsewhere, but prefer OLLAMA_API_BASE_URL
OLLAMA_URL="http://localhost:11434" 

# n8n Webhooks
N8N_APPOINTMENT_WEBHOOK_URL="YOUR_N8N_APPOINTMENT_WEBHOOK_URL_HERE"
# N8N_WEBHOOK_URL below is a generic one, specific ones like above are preferred
N8N_WEBHOOK_URL="https://n8n.artemis-ai.ca/webhook/mediconnect" 

# Public variables (can be exposed to the client-side)
NEXT_PUBLIC_OLLAMA_URL=${OLLAMA_API_BASE_URL} # Use the more specific Ollama URL
# NEXT_PUBLIC_N8N_WEBHOOK=${N8N_WEBHOOK_URL} # This might be too generic, consider if needed

# Patient Session JWT
PATIENT_JWT_SECRET="your-default-patient-secret-key-for-dev" # Secret for signing patient session JWTs