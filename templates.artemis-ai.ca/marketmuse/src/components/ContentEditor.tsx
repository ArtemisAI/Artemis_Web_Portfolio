/**
 * TODO:
 * - Render content editor UI (title, body, channel selection)
 * - Connect to /api/content for drafts and schedules
 */
export default function ContentEditor() {
  return <div>ContentEditor (TODO)</div>;
}